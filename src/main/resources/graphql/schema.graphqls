type Book {
  id: ID!
  title: String!
  description: String
}

type Query {
  getAllBooks: [Book]!
  getBookById(id: ID!): Book
}

type Mutation {
  createBook(title: String!, description: String): Book
}