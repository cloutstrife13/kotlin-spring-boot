type Book {
  id: Int!
  title: String!
  description: String
}

type Query {
  getAllBooks: [Book]!
  getBookById(id: Int!): Book
}

type Mutation {
  createBook(title: String!, description: String): Book
}